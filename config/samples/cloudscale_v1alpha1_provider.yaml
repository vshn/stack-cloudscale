apiVersion: cloudscale.syn.vshn.ch/v1alpha1
kind: Provider
metadata:
  name: cloudscale-provider-sample
spec:
  credentialsSecretRef:
    name: test-credentials
    key: token
    namespace: crossplane-cloudscale
